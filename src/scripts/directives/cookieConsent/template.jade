section.reverse.bg-blue.cookie-consent(ng-hide="consent()")
  .container
    .row.flex-center
      .col-sm-9.col-xs-12
        p This website uses cookies to ensure you get the best experience on our website.
          = ' '
          a(ui-sref="site.page({page:'privacy'})") Learn More.
      .col-sm-3.col-xs-12
        button.btn.btn-xs.btn-block.btn-secondary(ng-click="consent(true)") Got it!
