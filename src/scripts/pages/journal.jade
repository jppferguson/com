.reverse.bg-green.header-offset.vh-50.relative
  .container.center-center
    h1 Journal
    p.lead I dabble in a bit of writing every now &amp; then.
    p Mainly about design, code, freelancing but sometimes just random shit.

section.section
  .container
    .row
      .col-lg-8.col-lg-push-2
        article.article-preview(ng-repeat="article in articles" ng-hide="isLoading")
          h2
            a(ui-sref="site.journal.post({slug: article.slug})" ng-bind-html="article.title.rendered") {{article.title.rendered}}
          p About a {{ article.readingTime }} read from
            = ' '
            em {{ article.date | date:format:'mediumDate' }}.
            a.tag(ng-repeat="tag in article.tags" ui-sref="site.journal.tag({tag: tag.name})" ng-show="article.tags[0]") \#{{tag.name}}
